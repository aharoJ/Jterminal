only for ALL
Mutator>ALL<Mutator

//comment any that are not IN the excel sheet
: leave the ones that are in the array [x,x,x]


crl+f = killed --> score
crl+f = survived --> score

mutation score is:
    kill / (kill/survived)? = mScore 